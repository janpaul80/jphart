{
  "name": "jphart-portfolio",
  "version": "1.0.0",
  "description": "Professional portfolio website for paulhartmann.dev",
  "main": "index.html",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "start": "python -m http.server 8000",
    "check": "node --check site.js && node --check hero-scene.js && node --check api/checkout.js && node --check api/contact.js && node --check api/auth/github.js && node --check api/auth/google.js && node scripts/validate-site.js && node scripts/test-api.js"
  },
  "keywords": [
    "portfolio",
    "full-stack",
    "developer",
    "web-development"
  ],
  "author": "Paul Hartmann",
  "license": "MIT",
  "dependencies": {
    "@vercel/analytics": "^2.0.1",
    "nodemailer": "^6.9.7"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
